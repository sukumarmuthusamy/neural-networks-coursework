## CIFAR-10 Classification using CNN

This assignment implements a convolutional neural network (CNN) to classify images from the CIFAR-10 dataset and evaluates its performance against fully connected neural networks.
